#if !defined(PANGRAM_HPP)
#define PANGRAM_HPP

#include <algorithm>
#include <map>
#include <string>

namespace pangram {

bool is_pangram(std::string input);

} // namespace pangram

#endif // PANGRAM_HPP
