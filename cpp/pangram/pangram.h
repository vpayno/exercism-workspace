#if !defined(PANGRAM_H)
#define PANGRAM_H

#include <algorithm>
#include <map>
#include <string>

namespace pangram {

bool is_pangram(std::string input);

} // namespace pangram

#endif // PANGRAM_H
