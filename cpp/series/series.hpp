#if !defined(SERIES_HPP)
#define SERIES_HPP

#include <string>
#include <vector>

namespace series {

std::vector<std::string> slice(std::string sequence, int span);

} // namespace series

#endif // SERIES_HPP
