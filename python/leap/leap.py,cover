> """
> Leap Year exercise
> https://exercism.org/tracks/python/exercises/leap
> """
  
  
> def leap_year(year):
>     """Checks to see if given year is a leap year."""
  
      # I don't get why they claim this is more readable.
>     return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)
